apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pkebs
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nextcloud-svc
            port:
              number: 80
